apiVersion: ptp.openshift.io/v1
kind: PtpConfig
metadata:
  name: client
  namespace: openshift-ptp
spec:
  profile:
  - name: "client"
    interface: "eno1"
    ptp4lOpts: "-s"
    phc2sysOpts: "-a -r"
  recommend:
  - profile: "client"
    priority: 4
    match:
    - nodeLabel: "ptp/client"
