---
apiVersion: apps.open-cluster-management.io/v1
kind: Channel
metadata:
  name: {{ channel }}
  namespace: {{ namespace }}
spec:
  type: GitHub
  pathname: {{ customer_repo }}
  secretRef:
    name: git-secret
{{ cert_clause | indent( width=2, indentfirst=True ) }}
