contentOrigin:
  # Define at least one source of packages, but you can have as many as you want.
  repofiles:
     - ./redhat.repo

packages:
  [bc, linuxptp, iperf3, realtime-tests] 

arches:
  - x86_64

context:
    containerfile: Dockerfile
